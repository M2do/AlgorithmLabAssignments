#include <bits/stdc++.h>
using namespace std;
int main()
{
	/*
	ifstream file;
	file.open("input.txt", ios::in);
    if(!file.is_open())
    {
		cout<<"error while opening ";
    }
    else
    {
		string line;
		while(file.good())
		{
			getline(file,line);
			cout<<line<<endl;
		}
    }
    */
    
	return 0;
}
